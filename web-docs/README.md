This directory contains the static W16 documentation site for GitHub Pages.

Main file:

> index.html

Styles:

> style.css

The `styel.css` file is kept as a compatibility redirect to `style.css`,
because it already existed with a typo in the file name.
