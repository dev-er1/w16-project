name: W16 CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

env:
  CARGO_TERM_COLOR: always

jobs:
  w16-ci:
    name: W16 & w16-langs CI
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Enable сache
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: |
            .
            w16-langs

      - name: w16-project cargo check
        run: cargo check

      - name: w16-project cargo build (Release)
        run: cargo build --release

      - name: w16-project cargo test
        run: cargo test

      - name: w16-langs cargo check
        working-directory: ./w16-langs
        run: cargo check

      - name: w16-langs cargo build (Release)
        working-directory: ./w16-langs
        run: cargo build --release

      - name: w16-langs cargo test
        working-directory: ./w16-langs
        run: cargo test
